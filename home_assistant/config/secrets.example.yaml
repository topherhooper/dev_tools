# Copy to secrets.yaml (git-ignored) and fill in real values.
# CI uses this file as-is, so every !secret referenced in config must exist here.
home_name: Home
home_latitude: 0.0
home_longitude: 0.0
home_elevation: 0
time_zone: UTC
