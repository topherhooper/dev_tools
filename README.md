# dev_tools

A monorepo of personal projects. Each top-level folder is self-contained, with its own
README, dependencies and Makefile.

| Project | Description |
| --- | --- |
| [`home_assistant/`](home_assistant/) | Home Assistant config-as-code and the `hactl` CLI |
| [`legacy/`](legacy/) | Older sandbox: TensorFlow Docker env and VM setup scripts |
